import React from "react";

// Componente não utilizado (reservado para geração de dados)
const PackingDataGeneration = (props) => {
  let totalPerSize;
  if (props.id.includes("box_from_input_")) {
  }
};

export default PackingDataGeneration;
